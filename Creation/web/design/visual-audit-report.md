# 视觉走查报告

## 参考源
- `604ac8bc3219b5eba7a248a83afd3c5d.jpg`
- `aa7aa25a5b9d9bcaa6e1c9d9ff788fb1.jpg`

## 像素级对照维度
- 配色：已切换为浅灰背景 + 奶白卡片 + 深色强调 + 橙黄色高亮体系。
- 字体：统一为 Inter + 中文回退字族，标题与正文行高分层。
- 圆角：按 8/12/16/24/32 分级统一。
- 阴影：按 soft/card/float 三层阴影统一。
- 间距：按 8pt 栅格重排所有主布局与组件间距。
- 图标：已替换为统一线性描边风格 SVG 图标。
- 按钮：统一主按钮、幽灵按钮、强调按钮三种形态与状态动画。
- 卡片：统一 `ui-card` 与 `bento-card` 语义结构。
- 动效：统一 220ms + cubic-bezier(0.22,1,0.36,1) 曲线。

## 差异标注与修复项
- 已修复：旧版深色渐变背景与参考图风格不一致。
- 已修复：旧版样式散落在多个 Vue 单文件内，现统一抽离到 `src/styles`。
- 已修复：旧版按钮/输入/标签样式不统一，现采用设计令牌驱动。
- 已修复：缺失 Modal、Table、Pagination、Toast 视觉体系，现已补齐。
- 已修复：缺失主题接口，现支持 `data-theme` 浅色/暗色切换。
- 已修复：缺失 reduced-motion 降级，现已在 `motion.css` 覆盖。

## 视觉回归执行方式
- 运行：`npm run test:visual`
- 产物：`tests/visual/dashboard.spec.js` + Playwright 快照

## 流程节点落地
- Day1：设计令牌抽取（已完成）
- Day2-4：组件与布局重构（已完成）
- Day5：视觉验收与性能优化（已完成基础项）
- Day6：测试与文档（已完成）
- Day7：灰度与回滚演练（需按部署环境执行）
